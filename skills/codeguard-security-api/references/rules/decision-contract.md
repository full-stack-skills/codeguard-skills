# 决策契约

## 焦点

身份、功能权限、对象层授权、文件上传和防重放。

## 硬性规则

不信任客户端 tenant/user/role 声明；资源归属必须在服务端从受信身份推导。

## 通用证据要求

- 记录真实作用域、输入来源、工具/规则版本和时间。
- 区分已验证事实、基于上下文的推断、建议和未知项。
- 任何自动修复或抑制都要求原失败证据和复验证据。
- 缺少工具、配置、权限或环境时写 `UNVERIFIED`，不写 `PASS`。

## 停止条件

- 目标作用域不清晰。
- 操作将覆盖用户未提交改动。
- 需要安装、外部发布、凭据、付费或破坏性动作但未获授权。
